export class Experience {
    constructor(id,company,role,startDate,endDate) {
        this.id = id,
        this.company = company,
        this.role = role,
        this.startDate = startDate,
        this.endDate = endDate
    }
}